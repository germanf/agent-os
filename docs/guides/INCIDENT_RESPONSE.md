# Incident Response

> **⚠️ Documentación pendiente**
> Este archivo documentaría procedimientos para responder a incidentes de producción (caída del servicio, HTTPS, firewall, etc.).
> Por ahora, ver [`DEPLOYMENT.md`](../../DEPLOYMENT.md) y los logs:
> - `sudo journalctl -f -u agentic-software-boutique`
> - `sudo journalctl -f -u nginx`
> - `tail -f dashboard/deploy.log`